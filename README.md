# Drone_payload_delivery
Autonomous drone payload delivery system using GPS waypoints and a Raspberry Pi as a flight computer. The drone follows preplanned missions, monitors telemetry via MAVLink, releases payloads at target locations using servo control, and returns safely with minimal human intervention.
